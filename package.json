{
  "name": "epd-playground-demo",
  "version": "0.0.0",
  "scripts": {
    "dev": "vite",
    "build": "vue-tsc --noEmit && vite build",
    "serve": "vite preview",
    "publishdemo": "vue-tsc --noEmit && vite build && git add dist -f && git commit -m \"test\" && git subtree push --prefix dist origin demo"
  },
  "dependencies": {
    "@i4mi/js-on-fhir": "^0.2.1",
    "uuid": "^8.3.2",
    "vue": "^3.2.16"
  },
  "devDependencies": {
    "@vitejs/plugin-vue": "^1.9.3",
    "typescript": "^4.4.3",
    "vite": "^2.6.4",
    "vue-tsc": "^0.3.0"
  }
}
